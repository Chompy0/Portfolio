# Portfolio page with Vue/TailwindCSS

This repository contains my portfolio page.

## Installation

1. To install dependencies, execute at the root of the repo `npm install`.
2. At the root of the repo again, run by executing ` npm run dev`.


